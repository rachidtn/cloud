{% extends 'base.html.twig' %}

{% block body %}
    <h1>Medecin</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medecin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medecin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ medecin.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ medecin.email }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ medecin.specialite }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ medecin.numero }}</td>
            </tr>
            <tr>
                <th>image</th>
                <td>{{ medecin.imageFile}}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('medecin_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('medecin_edit', { 'id': medecin.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
        <li>
            <a href="{{ path('medecin_edit', { 'id': medecin.id }) }}">supp</a>
        </li>
    </ul>
{% endblock %}
